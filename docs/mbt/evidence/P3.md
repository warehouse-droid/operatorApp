# MBT Phase 3 Evidence Report — Tier 3

Status: **AUTOMATED COMPLETE / PHYSICAL IPHONE AND LIVE PILOT PENDING**.

One entirely fresh authoritative `cd server && bash tools/mbt-gauntlet.sh P3`
run completed successfully after the final source/test edit. The machine
summary records `status: passed`, exit code 0, start
`2026-08-04T06:29:35Z`, and finish `2026-08-04T07:08:21Z`.

- Spec approval: obtained from the user on 2026-08-03.
- Approved spec: `docs/mbt/phase-3-local-operations-shadow-billing.md` and
  `docs/mbt/03-executable-spec.md`.
- Branch: `codex/dockerVer`.
- Base commit: `37dfafbf534268aa84832c0f7a677f5f6a656bf6`.
- Source state: uncommitted Phase 3 working tree; the final gauntlet's source
  diff-integrity check passed after every persisted mutant restored its exact
  source hash. No commit or push was requested or performed.
- Toolchain: existing exact-pinned `server/package-lock.json` and isolated
  Docker test images; no Phase 3 dependency addition approved.
- Machine artifacts: `server/test-artifacts/gauntlet-summary.json`,
  `server/test-artifacts/coverage/coverage-summary.json`, and
  `server/test-artifacts/playwright/report.json`.
- Automated-gauntlet scope: synthetic/disposable data only. A later, separately
  authorized controlled application recreation is recorded below. No Phase 3
  feature activation or external NetSuite/Samsara write was performed.
- Post-gauntlet isolation audit: no container remained under Compose project
  `mbbs-mbt-p1-test`; `git diff --check` passed; the application, database, and
  Ollama production container IDs, creation timestamps, and start timestamps
  matched their pre-run identities byte-for-byte.

## P3.1 safety infrastructure

The final focused isolated run passed 43/43 gate, migration, predeploy,
configuration, and disabled Dispatch/Driver tests. Typecheck and
lint/complexity passed. The persisted P3 selection killed 52/52 mutants with
source hashes restored exactly. This is packet evidence only, not final Phase
3 completion evidence.

## Pre-implementation non-regression baseline

The isolated command
`MBT_GAUNTLET_SKIP_REGISTRY_AUDIT=1 bash tools/mbt-gauntlet.sh P2` completed
with exit 0 before any Phase 3 runtime edit. It passed 407/407 main tests,
shuffled repetition, types, lint, syntax, coverage, P2 mutation, licenses,
106/106 legacy harnesses, production-shaped closed-gate checks, and 60/60
Playwright cases across desktop Chromium, mobile Chromium, and mobile WebKit.
This is a baseline only, not Phase 3 completion evidence. Registry audit was
explicitly skipped; final evidence must run the full dependency audit unless
the environment makes it independently unavailable and records that limitation.

## Spec to test mapping

Every detector below ran in the fresh 1,104-test ordered suite, all three
fresh-database shuffled repetitions, and the coverage run. Browser detectors
also ran in the final three-project Playwright matrix.

| Frozen requirement | Primary executable detector(s) | Final |
|---|---|---|
| P3-F01 NetSuite full/incremental customer sync | `integration/customer-sync.test.js`, `concurrency/customer-sync-races.test.js` | pass |
| P3-F02 source ordering/conflict | `integration/customer-sync.test.js`, `property/customer-sync.property.test.js` | pass |
| P3-F03 failed snapshot preservation | `integration/customer-sync.test.js` | pass |
| P3-F04 source/consumer compatibility | `integration/customer-consumer-compatibility.test.js` | pass |
| P3-F05 Returns compatibility/cutover | `integration/customer-consumer-compatibility.test.js`, legacy Returns harnesses | pass |
| P3-F06 current workbook preview | `unit/customer-spreadsheetml.test.js` | pass |
| P3-F07 workbook apply/retry | `integration/master-data-import-foundation.test.js` | pass |
| P3-F08 hostile imports | `property/master-data-import.property.test.js`, SpreadsheetML/CSV boundary suites | pass |
| P3-F09 local item setup | `integration/local-item-catalog-p3.test.js`, local-import suites | pass |
| P3-F10 shared yards/fleet/master setup | `integration/shared-dispatch-mbt-yards.test.js`, `integration/dispatch-truck-types.test.js` | pass |
| P3-F11 asset registration/import | asset registry/import integration, concurrency, HTTP, and browser suites | pass |
| P3-F12 rate-card setup/calculation | rate-card integration/concurrency/CSV suites and `property/local-rate-calculator.property.test.js` | pass |
| P3-F13 Front Desk conversion | `integration/frontdesk-workflow.test.js`, `concurrency/frontdesk-races.test.js`, browser suite | pass |
| P3-F14 immutable snapshot/amendment | Front Desk workflow/adversarial and browser suites | pass |
| P3-F15 current contract front leg only | `integration/bin-contract-front-leg.test.js`, Dispatch browser suite | pass |
| P3-F16 atomic leg plan/reservation | BIN Dispatch integration/concurrency/HTTP/browser suites | pass |
| P3-F17 leg advancement/recovery | `concurrency/bin-leg-advancement-races.test.js`, BIN hardening suites | pass |
| P3-F18 Driver online BIN delivery | Driver execution/pilot/authorization and browser suites | pass |
| P3-F19 offline/reopen/sync timestamps | offline queue, integrity, race, time-evidence, and browser suites | pass |
| P3-F20 changed-manifest review | Driver execution and coverage-hardening suites | pass |
| P3-F21 loaded pickup/dump | loaded-exchange, reservation, manifest-integrity, and receipt suites | pass |
| P3-F22 exchange | Driver execution, template-state, property, and browser suites | pass |
| P3-F23 movement comparison | pilot reconciliation and asset registry suites | pass |
| P3-F24 receipt/distance comparison | pilot reconciliation, hardening, and rate-calculation suites | pass |
| P3-F25 MBT contract calculation | local billing unit/property and shadow-billing suites | pass |
| P3-F26 MBBS cross-charge rules | shadow-billing, completed-load, property, and P3.11 E2E-D suites | pass |
| P3-F27 local approval only | shadow-billing integration/concurrency/HTTP/browser suites | pass |
| P3-F28 audited correction lineage | pilot reconciliation and billing browser suites | pass |
| P3-F29 independent closed gates/recovery | `integration/p3-feature-gates.test.js`, predeploy/runtime restart checks | pass |
| P3-F30 complete non-regression | full P3 gauntlet, 106-harness legacy allowlist, three browser projects | pass |

## Final fresh gauntlet

| Layer | Command | Result |
|---|---|---|
| Tests | isolated ordered Node suite | **187 files / 1,104 tests passed** |
| Types | `npm run typecheck:mbt` | pass; 0 diagnostics |
| Lint/complexity | `npm run lint:mbt` | pass; 0 errors/warnings |
| Legacy public syntax | `npm run syntax:legacy` | pass |
| Coverage | `npm run test:mbt:coverage` | **97.77% statements, 90.82% branches, 99.34% functions, 97.77% lines**; unchanged 95/90/95/95 thresholds passed |
| Mutation | base plus dedicated/extended persisted runners | **182/182 killed** across the 70-base set and all declared P3/Browser packs; 100%; exact source hashes restored |
| Property | property suites inside the ordered, shuffled, and coverage runs | pass; includes the frozen 1,000-case and import/customer permutation contracts |
| Concurrency/failure injection | integration/adversarial/concurrency categories | pass; atomic rollback, exact retry, multi-client races, and disposable DB isolation included |
| Browser/accessibility | Playwright desktop Chromium, Android-sized Chromium, iPhone-sized WebKit | **104 executable cases passed, 1 disclosed WebKit skip, 0 failed/flaky/unexpected** |
| Full legacy non-regression | explicit full baseline | **106/106 harnesses passed** |
| Production-shaped execution | pinned omit-dev image, migrations/preflight, authenticated closed-gate smoke, restart and repeat | pass; migrations 001–121 and post-restart recovery verified in isolation |
| Supply chain/licenses/secrets | dependency tree, license allowlist, change/secret scanner, `npm audit` | 383 packages accepted with the recorded pre-existing `buffers@0.1.1` metadata exception; 355 new paths scanned; **0 vulnerabilities** |
| Suite health/shuffle | fresh DB seeds `2026080301`, `2026080337`, `2026080399` | **3 × 187 files / 1,104 tests passed** |
| Source integrity | gauntlet before/after diff and persisted runner hashes | pass |

## Manual/live gates

- Live NetSuite customer synchronization: not authorized during implementation.
- Real customer workbook import: not authorized during implementation.
- Production application recreation: completed under separate authorization;
  all Phase 3 operational gates remain closed.
- Phase 3 feature activation: pending a separately named/staged pilot scope.
- Physical Android Chrome and iPhone Safari workflows: not yet performed.
- Operational pilot/reconciliation: pending separate deployment authorization.
- Linux Playwright WebKit cannot persist Blob/File values in IndexedDB; validate
  this photo substep on physical iPhone Safari.

This evidence is labelled
`AUTOMATED COMPLETE / PHYSICAL IPHONE AND LIVE PILOT PENDING`; never
`PILOT COMPLETE` without the live manual evidence defined by the approved
Phase 3 plan.

## Controlled production application recreation — 2026-08-04

The user separately authorized recreating the container on `codex/dockerVer`
so the deployed Phase 3 surfaces could be inspected. This authorization did
not enable a Phase 3 operation, NetSuite write, Samsara write, live import,
pilot allowlist, commit, or push.

- The previous healthy image
  `sha256:84ae2b9aec92b75b5bd0c8a8b0d237229311e31f99ec391dc9660af3b373c05d`
  is retained as both
  `mbbs-operator-app-app:pre-mbt-p3-20260804T120927Z` and the corresponding
  migration rollback tag.
- The exact production-equivalent image accepted by the fresh P3 gauntlet,
  `sha256:58c97d3471f983c9ea1c89a13efff2c87265b4dbc7ee22d4600db61d8358df14`,
  was tagged directly as the Compose app/migration image and as
  `mbt-p3-58c97d34`; it was not rebuilt after certification.
- Fresh backup
  `docker/backups/mbbs-before-mbt-p3-20260804T120927Z-4K2qmB/production.dump`
  is 544,238,513 bytes, mode 600, with SHA-256
  `273c6b8020a663b398f2ecddc41cc482169f32f7aa4946eb6afe62cbb3f38b7c`.
  Container and host hashes matched, `pg_restore --list` passed, and a complete
  `--exit-on-error` restore into a PostgreSQL 18 container with networking
  disabled passed. Restored aggregates exactly matched the backup: 110
  migrations, 43 current Dispatch snapshots, 1,473 history snapshots, 10
  operators, 8 trucks, 5 local items, zero enabled MBT flags, 5,620 Sales
  Orders, 681 Purchase Orders, and 620 Transfer Orders. The temporary restore
  container and volume were then removed; the protected host backup remains.
- Only `app` was stopped. PostgreSQL and Ollama remained healthy with their
  original start timestamps and zero restarts. Immediately before migration
  there were zero other active and zero idle-in-transaction database sessions.
- Migrations 110–121 applied once from the accepted image with a five-second
  lock timeout and fifteen-minute statement timeout. The ledger now contains
  122 migration rows, including exactly 12 Phase 3 rows. All nine required
  Phase 3 flags exist and remain false.
- The pre-cutover and post-start read-only P3 preflights both exited 0. The
  captured post-start result was exactly `ready: true`, with empty
  `missingMigrations`, `enabledFlags`, `dispatchCollisions`, `missingFlags`,
  `unexpectedFlags`, `duplicateFlags`, and `nonFalseFlags` arrays.
- Seven net new mirrored Sales Orders arrived during the online backup window;
  the last was synchronized at `2026-08-04T12:23:33.5161Z`, before `app`
  stopped at `2026-08-04T12:24:28Z`. This was normal established synchronization,
  not a migration effect. Stable operational aggregates remained 43 current
  snapshots, 1,473 history snapshots, 10 operators, 8 trucks, and 5 local
  items.
- The recreated container ID is
  `49332a2b6025601cf960c10331841d52ffb859dbf27fec5262436ff52f39693d`.
  It runs the accepted image, is healthy, has zero restarts, was not OOM-killed,
  and has no container error. `/`, `/control`, `/operator`, `/dispatch`, `/scm`,
  `/sales`, `/driver`, `/mbt/frontdesk`, `/mbt/billing`, `/mbt/assets`,
  `/mbt/config`, `/health`, and `/api/driver/client-version` returned 200;
  anonymous `/api/mbt/status` returned 401.
- The live process resolved the MBT root, NetSuite writes, customer sync,
  master data, asset management, Front Desk, BIN Dispatch, Driver execution,
  and billing-operation gates all to false. Database counts remained zero for
  contracts, service visits, BIN assets, MBT outbox rows, and billing versions.
- The isolated authenticated runtime-smoke helper intentionally refused the
  live stack because it requires `MBT_TEST_ISOLATED=1`; it failed before
  inserting its temporary operator, and the database confirmed zero such
  fixtures. This safeguard was not bypassed. The supported live read-only
  preflight and route/authentication matrix passed instead.
- Startup logs contain the previously recorded rejected printer-agent message,
  `Valid enabled printer-agent credentials are required.` No fatal, unhandled,
  uncaught, OOM, connection-refused, or migration-failure signal was found.

The deployment exposes the current Phase 3 application code for inspection,
but its operational controls remain intentionally locked. A staged pilot still
requires named customers/operators, Bin truck/driver/assets/date scope, and
explicit feature-gate authorization.

## P3.2 import foundation — packet evidence

A fresh disposable database applied migrations 001–112 and 115, and all six
frozen P3.2 files passed **31/31**. This includes bounded CSV and SpreadsheetML,
4,000 property examples, a synthetic 1,262-row scale preview, 25 revision
races, HTTP authorization/gates, exact replay, and post-canonical transaction
rollback. The full Phase 3 typecheck exited 0 at this packet checkpoint. The
previous cumulative mutation run killed **56/56** mutants with exact source
restoration; the expanded final Phase 3 mutation set remains pending later
packets. No real workbook or live NetSuite read/write occurred.

### P3.2 supplied-workbook compatibility hardening — 2026-08-03

- A read-only, network-disabled parse of `/home/ubuntu/MBT_customer_20260803.xls`
  initially failed safely with `MBT_IMPORT_SPREADSHEETML_STRUCTURE_INVALID`.
  No row was returned or imported. Element-name inspection showed that the
  NetSuite-authored SpreadsheetML includes benign `Company` document metadata,
  which the synthetic allowlist had omitted.
- A frozen synthetic detector reproduced only that metadata shape. It produced
  **8 passed / 1 failed** before the change. The parser now admits the single
  non-executable `Company` element while retaining the existing DTD/entity,
  formula, external-link, macro, namespace, nesting, and bare-ampersand guards;
  `Company` is accepted only as a direct `DocumentProperties` child; a second
  RED detector proved the initially broad placement check was unsafe before
  the parent constraint was added. The same focused suite passes **9/9**.
- The corrected aggregate-only preview used the workbook's exact hierarchy
  label, `Mr.Bin Holdings Group Inc. : Mr Bin Trucking Inc.`, and returned
  **1,262 total / 1,251 eligible / 11 skipped** rows: 10 other-subsidiary rows,
  one non-customer-status row, 39 eligible rows with an incomplete entity
  number, 182 eligible rows with blank email, 2 eligible rows with blank phone,
  and one bounded Data-node ampersand repair. File SHA-256 is
  `b4fd35e134626543d3e94cbbb0ef898f99dd168b9452eef64986dd8a4f3b95dc`;
  normalized eligible-row hash is
  `3a2a507b84c5bd1faf1a99f5a415f88168b9fe7338bc7e1a5b8e90c4b5031955`.
  No customer row, contact value, or address was printed, persisted, or copied
  into Git, and no import/apply operation ran.
- The structurally equivalent synthetic 1,262-row fixture now represents both
  the overall review counts (47/187/11) and the filtered eligible counts
  (39/182/2). A persisted P3 mutant removes `Company` from the allowlist so the
  final mutation gauntlet must prove this compatibility boundary remains live.

## P3.4 local references and shared Dispatch masters — packet evidence

This is append-only packet evidence, not the final fresh Phase 3 gauntlet.

- Fresh migration command: `docker compose -p mbbs-mbt-p34-test -f
  docker-compose.mbt-test.yml --profile tools run --rm migrate` — exit 0,
  migrations 001–112 applied from an empty disposable PostgreSQL database.
- P3.4 integration/concurrency command: `node --test
  --test-concurrency=1 --test-reporter=spec` over the migration-112 upgrade,
  shared-yard, truck-type, 25-race capability, and local-item/master files —
  **15/15 passed**, exit 0.
- Legacy local-item and disabled-BIN command — **23/23 passed**, exit 0.
- Established Dispatch fleet/setup, save-coordination, and endpoint harnesses
  all exited 0; the endpoint harness reported **18 checks passed**.
- Focused ESLint and `git diff --check` exited 0. The shared full typecheck had
  zero P3.4-owned diagnostics but was blocked by concurrent P3.2/P3.5 source
  diagnostics, so it is not represented as a globally passing layer here.
- Exact-rerun evidence: representative schema-111 legacy truck backfilled to
  Flatbed with its legacy checksum retained; exact migration SQL rerun retained
  the full P3.4 data digest. Independent digest was
  `7c385256d9038b16732cd312bc3acb1e` both before and after.
- Known test-environment limit: a long-lived disposable database containing
  synthetic unmapped yards fails 112 at the NOT NULL bridge, by design. A real
  Dispatch/NetSuite location ID must never be fabricated. Fresh projects and
  exact mapped upgrade fixtures are the supported automated evidence path;
  production deployment still requires the explicit shared-yard preflight.
- No dependency, deployment, restart, flag activation, Driver, Operator,
  Smart SCM, external request, or live-data change occurred in this packet.
  P3.4 browser/mutation/final-gauntlet evidence remains pending.

## P3.3 canonical customer synchronization — packet evidence

This is append-only packet evidence, not the final fresh Phase 3 gauntlet.

- Frozen command: `node --test --test-concurrency=1 --test-reporter=spec`
  over `property/customer-sync.property.test.js`,
  `integration/customer-sync.test.js`,
  `concurrency/customer-sync-races.test.js`, and
  `integration/customer-consumer-compatibility.test.js` — final result
  **15/15 passed**, exit 0. The run includes 50 database races, injected atomic
  rollback, 2,500 fast-check cases, signed event replay, bounded snapshot
  consumption, zero direct NetSuite/transport mutations, and exact Returns and
  `netsuite-mirror/v1` compatibility contracts.
- First GREEN was **13/15** and exposed conflict SQL parameter typing plus
  internal/public action-name mapping. Production was repaired without editing
  frozen tests; a focused 8/8 rerun and the final 15/15 rerun passed.
- Focused ESLint over all six P3.3 production files exited 0 with zero warnings.
  P3.3-owned files had zero diagnostics in the full MBT typecheck; unrelated
  concurrent asset/import diagnostics prevented representing that shared run
  as a global pass.
- Legacy compatibility: `npm run test:returns-customer-directory` passed;
  `npm run test:netsuite-mirror` passed using the baseline service's required
  repository mounts. The independent customer contract did not alter
  `netsuite-mirror/v1`.
- Import/canonical integration was rerun on fresh project
  `mbbs-mbt-p3-customer-final` after migrations 001–112 and 115: the four-file
  import property/integration/concurrency command passed **16/16**, exit 0.
  The same command on the long-lived shared database first passed 15/16 and
  failed only because a previous successful synthetic test had retained fixed
  entity number `741011`; PostgreSQL identified that earlier synthetic row.
  No assertion was weakened and no retained evidence was deleted to force a
  pass.
- The long-lived shared database also could not advance unrelated migration
  112 over synthetic unmapped yards; migration 111 was independently confirmed
  committed, while the fresh project applied 112 cleanly. This is disclosed as
  disposable-environment residue, not omitted or reported as GREEN.
- No live workbook, real customer PII, external request, posting/outbox work,
  feature activation, dependency, deployment, restart, SCM, Dispatch, Driver,
  or Operator change occurred in this packet. Coverage, mutation, shuffle,
  browser, supply-chain, and final P3 gauntlet evidence remain pending.

## P3.6 local rate cards and pure shadow-rate calculation — RED evidence

This is append-only RED evidence, not implementation, GREEN, deployment, or
final Phase 3 completion evidence.

- The established rate foundation control passed **30/30**, exit 0, covering
  pure P1 contracts, rate lifecycle, and immutable financial evidence before
  the new detectors were introduced.
- Six new files freeze **22 assertions**: 5 pure unit, 3 property, 6 database
  integration, 2 concurrency, 3 HTTP, and 3 browser/static contracts. They
  cover raw-metre half-open distance bands; safe integer CAD cents;
  deterministic line conservation; dump charge/cost/margin separation;
  manual/multi-CSV parity; atomic child graphs; hostile inputs and references;
  validation, activation, exact replay, used-version cloning; activation
  races; live Admin authority, no-store, gates, server-owned identity and zero
  transport; and accessible, focus-preserving local configuration.
- Focused RED command: `node --test --test-concurrency=1
  --test-reporter=dot` over the six files above in disposable Compose project
  `mbbs-mbt-p34-test`. Observed output contained exactly 22 `X` markers:
  **0 passed / 22 failed**, exit 1. All failures were intended missing behavior:
  8 calculator/export assertions, 8 configuration-service/export assertions,
  3 route `404` assertions, and 3 incomplete browser-contract assertions.
  There was no collection, syntax, fixture, PostgreSQL, timeout, or external
  transport failure.
- The 1,000-case property and 25-repetition race limits are frozen for GREEN;
  the current RED correctly stops at the missing operation export before those
  bodies execute. The assertions were not edited after RED was observed.
- Focused ESLint over all six files exited 0 after one pre-freeze test-variable
  rename; full `npm run typecheck:mbt` exited 0; and `git diff --check` exited
  0. No production file, dependency, migration, route, server/sidebar/UI
  implementation, feature flag, deployment, restart, or operational module was
  changed. Fixtures are synthetic; no real workbook/PII, NetSuite request,
  SCM, Dispatch, Driver, Operator, billing posting, or external call occurred.

## P3.7 Front Desk vertical slice — RED evidence

This is append-only RED evidence, not implementation, GREEN, deployment, or
final Phase 3 completion evidence.

- Frozen files: `test/mbt/support/frontdesk-fixtures.js`,
  `test/mbt/integration/frontdesk-workflow.test.js`,
  `test/mbt/concurrency/frontdesk-races.test.js`, and
  `test/mbt/e2e/p3-frontdesk.spec.js`. Their 10 top-level detectors cover
  P3-F13/P3-F14 quote lifecycle, exact-cent server pricing, atomic conversion,
  delivery/return precedence, local billing-case creation, immutable
  snapshots, return-only extensions, exact replay/conflict/rollback, 25+25
  independent races, focus retention, responsive timeline, accessibility, and
  absence of posting controls/artifacts.
- The exact Node RED command used `node --test --test-concurrency=1
  --test-reporter=spec` over the workflow and race files. It collected all
  **8/8** tests, then reported **0 passed / 8 failed**, exit 1, solely at the
  named missing `frontdesk-service.js` operations. No syntax, collection,
  PostgreSQL, fixture, timeout, or external-system error occurred.
- Isolated project `mbbs-mbt-p3-frontdesk-red` migrated 001–112 and 115 from an
  empty database. The synthetic prerequisite builder executed independently
  and proved `quotes=0`, `contracts=0`, and `outbox=0` after setup.
- The production-shaped Chromium run collected both browser tests and reported
  **0 passed / 2 failed**, exit 1. Both failed quickly and explicitly at the
  absent real Front Desk heading/workflow. Playwright's accessibility snapshot
  proved the actual authenticated page remained `Front Desk foundation` /
  `Phase 1` / `Front Desk is not yet operational`; auth and browser setup were
  healthy.
- Focused ESLint exited 0 with zero warnings and full `npm run typecheck:mbt`
  exited 0. No production implementation, migration, dependency, central
  router/server/sidebar, capability activation, deployment, restart, SCM,
  Dispatch, Driver, Operator, live customer data, NetSuite call, outbox work,
  or posting action was introduced by this RED packet.

## P3 configuration and typed shared-fleet exits — packet evidence

This is append-only packet evidence, not deployment or final Phase 3 evidence.

- Customer/config/API command: **22/22 passed** on disposable PostgreSQL.
  Combined new Phase 3 and established Local Items UI contracts: **7/7
  passed**. Existing local-item and closed-BIN controls: **23/23 passed**.
- Typed shared-yard/fleet command: **19/19 passed**, covering migration 112,
  exact-rerun upgrade, four shared yards, local masters, Settings UI, 25
  optimistic races, Flatbed default-yard preservation, and legacy Bin yard/FK
  coherence. The yard-coherence detector was observed RED at **4/5** before
  the source fix and GREEN at **5/5** afterward.
- Production-shaped capability HTTP hardening was observed RED on role-denied
  cache privacy, then GREEN **1/1**. It covers authentication, Dispatcher role,
  no-store, closed master-data gates, mandatory idempotency, server-bound actor,
  exact replay, stale revision, and one durable audit/history record.
- Existing Dispatch fleet static and save-coordination harnesses passed; fleet
  endpoint harness passed all **18** checks. Full Phase 3 typecheck, focused
  lint, Dispatch/MBT browser syntax, and whitespace checks exited 0 at this
  checkpoint.
- All runs used synthetic identities and disposable databases. No deployment,
  restart, feature activation, live workbook/PII, external request, NetSuite
  write, or SCM/Driver/Operator operational mutation occurred.

## P3.6 local rate cards and pure shadow-rate calculation — GREEN evidence

This is packet evidence, not deployment or final Phase 3 pilot-readiness
evidence.

- The implementation is local-only and additive: pure integer-cent/raw-metre
  calculations, transactional rate-card graph persistence, revision-bound
  validate/activate/clone commands, Admin/no-store APIs, and an isolated Rate
  Cards configuration surface. The existing MBT root plus `masterData` gates
  remain closed by default. Existing SCM, Dispatch, Driver, Operator, posting,
  outbox, and external transports are not called by this packet.
- Frozen GREEN was reached without editing the six RED files or their **22
  assertions**. An initial **7/8** database result exposed clone creation audit
  evidence using the source revision. The production audit was corrected to
  the new clone's own `1 -> 1` identity, after which the database/race set
  passed **8/8** and the exact six-file frozen set passed **22/22**.
- The final non-regression command combined those 22 assertions, the existing
  30 rate-foundation assertions, and nine new hardening assertions. It emitted
  **61 dots / 61 passed / 0 failed**, exit 0. The executed evidence includes
  3,000 property cases, 25 activation races, 25 simultaneous exact retries,
  live HTTP boundaries, browser/static focus preservation, exact cent
  conservation, separate dump charge/cost/margin, atomic graphs, and immutable
  used-version cloning.
- Separate hardening tests cover malformed envelopes and every child kind,
  currencies, unsafe values and overflow, fixed/percentage branches,
  authorization/gates, missing/stale/used lifecycle states, service-template
  references, pagination bounds, and controlled malformed-band rejection.
  Final focused coverage: **99.51% statements, 99.51% lines, 100% functions,
  93.71% branches** overall; calculator **100/100/100/90.52** and service
  **99.35/99.35/100/95.06** for statements/lines/functions/branches.
- Persisted selection added four money/concurrency/allowlist mutants. The
  writable ephemeral P3 mutation command killed **64/64 (100%)**, including
  all four P3.6 mutants, with source restoration hashes verified after each
  run.
- Focused ESLint over production, frozen/hardening tests, and the mutation
  runner exited 0; `node --check public/mbt-shell.js` exited 0. Full shared
  typecheck reported no P3.6-owned error but is disclosed as nonzero on
  concurrently edited Front Desk and unified-import services. The current
  five-file browser control is a selection preview; a complete multi-CSV
  parser/apply workflow remains separately owned.
- Final source hashes include calculator
  `20b6e97a2da0f23154f7b638a9d119a267ebf730bc24ef1beba5ac814d2943a8`
  and configuration service
  `b31d285a34dad7916d42caf08b731f6f916b72254222c1349cdc794e4d692c89`.
  All fixtures were synthetic and disposable. No dependency, migration,
  deployment, restart, commit, live import/PII, external request, or feature
  activation occurred.

## P3.6a five-file rate-card CSV import — specification checkpoint

- Spec approval: explicitly assigned by the parent implementation agent. Tier
  3 applies because hostile file parsing, money, authorization, idempotency,
  and multi-table atomicity are involved.
- The frozen target is a dedicated Admin/master-data/no-store preview/apply API
  over exactly five bounded CSV files. Server parsing creates the canonical
  graph; preview retains only import evidence; apply reuses the established
  audited local draft path inside one transaction; exact retries and races
  create one draft; and the existing Rate Cards controls become functional.
- Setup adds no dependency, migration, feature activation, deployment,
  restart, commit, or external access. RED/GREEN, coverage, persisted mutation,
  type/lint, browser syntax, concurrency, rollback, and existing rate controls
  are required before this checkpoint can become GREEN evidence.

## P3.7 Front Desk vertical slice — GREEN evidence

This is automated packet evidence, not deployment or live-pilot completion.

- Additive production boundaries are migration
  `113_mbt_p3_frontdesk_contract_chain.sql`,
  `src/mbt/frontdesk-service.js`, gated/no-store Front Desk routes in
  `src/mbt/router.js`, and the isolated `public/mbt-frontdesk.*` surface. Both
  Front Desk gates remain false by default. Default runtime distance/tax
  adapters fail closed; NetSuite posting endpoints and browser controls do not
  exist in this slice.
- The exact frozen Node command over `frontdesk-workflow.test.js` and
  `frontdesk-races.test.js` passed **8/8**, exit 0. It includes 25 independent
  two-client quote-conversion races, 25 independent extension races, exact
  replay, injected rollback after contract and visit insertion, immutable
  snapshots, explicit delivery-before-return identity, one local billing case,
  and unchanged counts in ordinary SO/PO/TO/Dispatch/Driver/Operator and MBT
  posting/outbox tables.
- The final coverage command adds the separate
  `frontdesk-adversarial.test.js`; all **16/16** assertions passed. Scoped c8
  for `frontdesk-service.js` reports **100% statements, 100% lines, 100%
  functions, 96.8% branches**, clearing the explicit 95/95/95/90 thresholds.
  The supplemental detector covers unauthorized/malformed input, bounded pilot
  search, active/effective/matching configuration, server distance/tax
  evidence, route fingerprint, CAD and safe-integer guards, every lifecycle
  conflict, successful/failed hooks, sparse projections, and invalid contract
  chains.
- The persisted command `node
  test/support/run-frontdesk-mutations.mjs`, guarded by
  `MBT_TEST_ISOLATED=1` and `MBT_MUTATION_EPHEMERAL=1`, killed **10/10 (100%)**
  targeted Front Desk mutants and verified the source hash after each restore.
  Mutants cover authorization, quote row locking, template/rate mismatch,
  effective dates, route hash, total-cent overflow, accepted-state conversion,
  premature return readiness, missing predecessor, and cancelled-contract
  extension.
- Production-shaped Chromium rerun passed **2/2** in **6.8 s**. It covers the
  accessible quote-to-contract workflow, retained search focus, absent posting
  actions, responsive mobile extension, immutable delivery timeline, no
  horizontal overflow, and zero axe serious/critical violations.
- The established regression command over feature gates, HTTP roles, router
  identities, disabled BIN Dispatch, and disabled/ordinary Driver projection
  passed **27/27**. The dedicated live Express Front Desk boundary passed
  **3/3**, covering private/no-store reads, roles and capability gates,
  server-owned actor/idempotency/pricing identities, and every new route.
  Focused ESLint over production/frozen/adversarial/mutation/browser files
  exited 0; all 13 owned implementation/test files are whitespace
  clean. Full MBT typecheck passed before concurrent packets changed the shared
  tree. The final rerun contained no Front Desk diagnostic and was nonzero only
  in concurrently edited asset/BIN Dispatch modules; that shared result is not
  represented as a global pass.
- The disposable `mbbs-mbt-p3-frontdesk-green` project applied the Front Desk
  migration chain from an empty PostgreSQL database; all identities/data were
  synthetic. No dependency, flag activation, deployment, restart, commit,
  live workbook/PII, NetSuite/Samsara call, external write, or operational SCM,
  Dispatch, Driver, or Operator mutation occurred. Result:
  `AUTOMATED COMPLETE / PILOT PENDING`.

## P3.6a five-file rate-card CSV import — RED evidence

- Seven new test/support files freeze **17 executable assertions**: three exact
  parser cases, two 1,000-example properties, four database/rollback boundary
  cases, two concurrency cases, three live HTTP authority contracts, and three
  browser contracts. Focused ESLint was clean before the assertions froze.
- The exact isolated command ran the six test files with concurrency one and
  dot reporting. Observed output was exactly **17 X markers / 0 passed / 17
  failed**, exit 1. Five failures stopped at the absent parser/property export;
  six stopped at absent preview/apply service exports; three received the
  intended dedicated-route 404; and three reached the missing real Apply/
  server-preview browser behavior. There was no collection, syntax, fixture,
  PostgreSQL, timeout, or external-system failure.
- Frozen assertion hashes: unit parser
  `e4d910a563e307ea9334516c6fbc18dabf917831b0f1bfae98a143b5ae5ca5d5`,
  property `cd6b95b8e5773fd67adaa0bb9a375b6fa6fc5de3a59149f4010b82c9166c41ee`,
  integration `3ed282e18d04fdcf86a5353f49c827ae9d6611372f26630e853790e92abeb1a2`,
  races `bc653a20183a90c0bb0877468d19ae0e746d6972cff7c1bc4cc27d0555b370b5`,
  HTTP `a0318a3c7691aae76b1445da0b1a03403beea0e3f6d514237c4c5275d4c0e83d`,
  UI `826c393a6e948fa8b6ebad768b68c7b374c2a53e4e2d5c8e7234f8105e7e17d1`.
  No production source was changed before this RED.

## P3.9 Driver PWA BIN execution — RED evidence

- Fresh disposable PostgreSQL project `mbbs-mbt-p39-red` applied migrations
  001–116, then executed the frozen property, integration, and concurrency
  files with `--test-concurrency=1 --test-reporter=dot`.
- Result: **nine intended failures, zero passes**, shown as exactly nine `X`
  markers. Every failure named a missing P3.9 operation; none arose from the
  synthetic Dispatch assignment fixture, PostgreSQL, collection, timeout, or
  an external service.
- The frozen packet covers 2,000 property examples, exact online/offline
  delivery application and replay, independent occurrence/receipt/application
  timestamps, wrong-asset and missing-evidence rejection, unsafe-snapshot
  review, 25-way exact retry, competing event IDs, and explicit zero generic
  order/NetSuite effects. Assertion hashes are recorded in the executable-spec
  journal. This is RED packet evidence only, not deployment or pilot readiness.

## P3.9 Driver PWA BIN client — RED evidence

- A fresh read-only disposable test container ran the frozen pure/static client
  contract with Node 20. It produced exactly **six intended failures and zero
  passes**: five named the absent `driver-bin-ui.js`, while the static shell
  assertion proved the file was not loaded or cached.
- The packet additionally freezes two Chromium workflows before production
  changes: online BIN start through only the local ledger, and airplane-mode
  scan/note/photo capture followed by reload and reconnect with the original
  occurrence time and exact evidence. Their frozen hashes are recorded in the
  executable-spec journal. This is RED evidence, not completion evidence.

## P3.6a five-file rate-card CSV import — GREEN evidence

This is automated packet evidence, not deployment or live-pilot completion.

- Additive production boundaries are
  `src/mbt/rate-card-csv-import.js`,
  `src/mbt/rate-card-csv-import-service.js`, two dedicated Admin/master-data/
  no-store routes, and the existing isolated Rate Cards configuration panel.
  The parser requires exactly `rate_cards.csv`, `distance_bands.csv`,
  `components.csv`, `dump_tariffs.csv`, and `deposit_rules.csv`; enforces 4 MiB
  per-file and 15 MiB aggregate ceilings; converts exact scalar types; sorts
  child identities; and delegates aggregate validation to
  `normalizeLocalRateCardGraph(..., {sourceKind: "csv"})`.
- Preview persists only one actor-owned private import batch and one normalized
  staged graph. Raw file bytes, rate-card domain rows, receipts, domain audits,
  outbox rows, and operational rows are absent. Apply locks and revalidates the
  batch, hashes, expiry, status, target token, staged graph, and references,
  then nests the established audited local-draft command and batch transition
  in one transaction. Exact retries replay; competitors cannot double-create;
  and post-draft injection rolls back nested and outer evidence together.
- The unchanged behavioral RED assertions reached **17/17 GREEN**. Eight
  separate post-GREEN adversarial assertions bring the focused result to
  **25/25**, including 2,000 generated property cases, 25 simultaneous exact
  retries, independent-key competition, live HTTP authority/no-store checks,
  binary inputs, limit override abuse, malformed actors/identities, staged
  evidence tampering, changed targets, missing references, and a deterministic
  PostgreSQL constraint-conflict translation with zero partial domain rows,
  plus a browser contract proving customer, generic local, and rate-card
  previews can unlock only their own Apply control.
- Two test-fixture-only corrections were made after the first database GREEN
  run: ordinary shared-database outbox assertions now subtract their captured
  baseline, and the expiry fixture moves both preview and expiry timestamps to
  a valid past interval so it respects the existing expiry-order constraint.
  Expected outcomes, codes, domain assertions, and production behavior were not
  weakened. This changes only the integration-file hash from its recorded RED
  value to
  `4e977dda6e03368b4b1712e1e8e6ff638533fcac16b784f57fdcd18104863724`.
  The other five frozen hashes remain exactly the recorded RED hashes.
- Scoped c8 clears Tier 3 at **100% statements, 100% lines, 100% functions,
  97.31% branches** overall. Parser coverage is **100/100/100/100** and service
  coverage is **100/100/100/93.05** for statements/lines/functions/branches.
  Four persisted mutants cover the exact-five-file guard, aggregate byte
  ceiling, preview ownership, and transactional rollback hook. The writable
  ephemeral cumulative P3 command killed **68/68 (100%)**, including all four,
  and verified source restoration after every mutant.
- The complete established local-rate foundation/manual suite plus CSV packet
  passed **86/86**, exit 0. The existing generic master-data import, race, HTTP,
  local-item/configuration-page set passed **20/20**, exit 0. Focused ESLint and
  browser syntax passed; `git diff --check` passed. Shared typecheck reported no
  diagnostic in any P3.6a-owned file and is honestly disclosed as nonzero only
  in concurrently developed `driver-bin-contract.js` and
  `driver-bin-execution-service.js`.
- Final production hashes are parser
  `f20e70d615ec8014fbe5fe2540d2ad2e14c0de6babccf8f43cff21d0cd3042d6`
  and service
  `a42ed4058f872b902269e06743fa2e5da4dad010b9b843f99e8d6f9dce5b2d3c`.
  All data and identities were synthetic and disposable. The root and
  `masterData` gates remain closed by default. No migration, dependency, flag
  activation, deployment, restart, commit, raw/live import, PII, external
  request, NetSuite posting, SCM, ordinary Dispatch, Driver, Operator, or
  billing mutation occurred. Result: `AUTOMATED COMPLETE / PILOT PENDING`.

## P3.8 current Dispatch BIN integration — GREEN packet evidence

This is Tier 3 automated packet evidence for P3-F15–P3-F17, not final Phase 3
completion, deployment, feature activation, or live-pilot evidence.

- The approved and previously observed RED inventory is unchanged. The final
  fresh disposable PostgreSQL run over the versioned feed contract, front-leg
  projection, atomic assignment, adversarial hardening, 50/25-way assignment
  races, advancement races, whole-leg moves, and audited recovery passed
  **33/33**, exit 0. A reverse-file-order repetition produced **33/33** pass
  markers, providing an explicit order-dependence check.
- P3-F15 passed with exactly one server-derived ready current front leg, the
  complete mandatory-stop route, a locked future timeline, bounded safe
  search, planned-leg timeline visibility, terminal-contract handling, and a
  database predecessor guard. Future visits, whole contracts, legacy orders,
  and incomplete server-owned asset identity never become draggable cards.
- P3-F16 passed one atomic plan/visit/reservation/snapshot/receipt/audit write,
  exact replay, stale and changed-payload conflicts, canonical database-owned
  type-Bin/size/base-yard capability checks, exact-asset identity, failure
  injection immediately after reservation, and 50 independent competitors
  yielding one complete winner. Ordinary order/dependency/Driver/Operator/
  NetSuite side-effect counts remained unchanged.
- P3-F17 passed whole mandatory-stop transfer, split/deleted/reordered-stop
  rejection, one revision winner for concurrent moves, started-leg rejection,
  audit-note-required recovery retaining the prior assignment, 25 independent
  advancement competitors, 25 exact advancement retries, one successor
  promotion, and one pool refresh. Completed and successor legs were never
  simultaneously exposed in the unassigned pool.
- The frozen default-off, schema, ordinary Driver/Dispatch, typed-fleet, and
  environment/database gate controls passed **39/39**. The first audit run was
  **37/38** because concurrent P3.9 work temporarily stopped the legacy default
  Driver projection from rejecting an assigned BIN job. The P3.9 owner restored
  assigned-scope rejection while adding the negative control that an unrelated
  driver's BIN assignment cannot block an ordinary driver; the final rerun
  passed **39/39**. No P3.8 assertion was weakened and no P3.8 production file
  was changed to resolve that concurrent regression.
- Production-shaped authenticated desktop Chromium passed **2/2**: one
  accessible draggable front-leg card rendered its complete route and locked
  future timeline, and one drag emitted exactly one visit-level command then
  materialized every mandatory stop as one non-splittable BIN group. Serious
  and critical axe violations were **0**. Mobile browser coverage remains part
  of the final combined P3 gauntlet rather than this desktop Dispatch packet.
- Focused c8 passed **30/30** and reported **99.8% statements, 90.3% branches,
  100% functions, and 99.8% lines** for `bin-dispatch-service.js`. The only
  uncovered source lines are 509–510, a defensive missing-BIN-type branch that
  cannot be reached through a valid visit because the database foreign key is
  `ON DELETE RESTRICT`; this is disclosed rather than adding a coverage-only
  assertion or corrupting database integrity.
- Persisted manual mutation `npm run mutate:mbt:p38` killed **6/6** plausible
  defects: future-search leakage, inverted truck capability, inverted exact
  asset identity, skipped post-reservation rollback, inverted whole-leg stop
  identity, and inverted successor eligibility. The runner used a fresh
  database per mutant and restored the source exactly to SHA-256
  `07ccf1f24fd479bd7337abf6275e22ab5bd37cf1bb32fb2832084d5ee19af358`.
- Focused project-policy TypeScript/JSDoc checking of
  `src/mbt/bin-dispatch-service.js` exited 0. The final shared full typecheck is
  honestly nonzero only in concurrently active P3.5 asset-import and P3.9
  Driver files; it reported no P3.8 diagnostic. Focused ESLint over the P3.8
  service, router, contracts, fixtures, Node tests, browser tests, and mutation
  runner exited 0 with zero warnings. `git diff --check` over the packet exited
  0.
- The isolated seven-harness legacy smoke baseline passed **7/7**. Its ordinary
  Dispatch load-assignment harness passed **133** checks, Driver-oriented
  schedule passed **50** checks, and fleet status, route duration, Admin, Smart
  SCM live-execution-gate, and targeted-order-sync harnesses all passed. The
  final Phase 3 gauntlet remains responsible for the complete legacy allowlist.
- A fresh schema applied migrations 001–117, including additive migration 116.
  Failure injection proved transactional rollback after reservation. No new
  dependency, external request, live data, NetSuite operation, production
  state mutation, deploy, restart, commit, push, or feature-flag activation was
  performed. P3.8 result: `AUTOMATED COMPLETE / PILOT PENDING`.

## P3.10 reconciliation and local shadow billing — RED evidence

- A fresh read-only Node 20 test container ran the frozen pure calculator and
  cross-charge packet before any P3.10 production module existed. The exact
  command produced **8 intended failures / 0 passes**, exit 1. All eight
  failures stopped at the named absent `calculateMbtLocalBilling` or
  `calculateMbbsCrossCharges` export; none came from collection, syntax,
  generated data, a timeout, PostgreSQL, or an external service.
- The frozen hashes at RED are unit calculator
  `fb7ce411db4bc73a2c7efa6d0f122d8dd6849ba400aaaad7c2e8f0204dc1fd91`,
  property calculator
  `f5160cdfbabd293144bd6772a4a2d2184cccd054079fa4a9b651418e492de991`,
  reconciliation integration
  `2bb60a5e9ae4cd000bcae49523ada81089fa083bc57e74d61693c70b06688509`,
  shadow-billing integration
  `4659284d315f0d8f63610a173fc1fcba61c34cbb2a23d39f0dbae2c6807d011e`,
  shadow-billing races
  `7f68acb8a34c1fc1aa6dd7d784c20b724d5d0789e419e1098c5f1b1afc007653`,
  and synthetic billing fixtures
  `b402639b58bfeafe6c10016883120b4a50f87f09094337927a81da6a47674252`.
  These product assertions are frozen; implementation may add separate
  adversarial detectors but may not weaken them.
- This is RED packet evidence only. No migration, production source, flag,
  dependency, deployment, live data, transport, NetSuite, Samsara, or
  operational module was changed by the run.
- A second fresh project applied migrations 001–119 to an empty PostgreSQL
  database and then ran the frozen reconciliation, shadow-billing, and race
  files serially. It produced **13 intended failures / 0 passes**, exit 1:
  every fixture completed and every assertion stopped only at an absent named
  P3.10 service export. This additionally proves the synthetic graph and
  additive migration collect cleanly before service implementation.

## P3.9 P3-F21/P3-F22 loaded pickup and exchange — GREEN packet evidence

This is Tier 3 server-scenario evidence for the approved P3-F21/P3-F22
acceptance criteria. It does not activate the Driver pilot or replace the final
combined Phase 3 gauntlet.

- Failure model and detectors: a customer-located BIN cannot be changed to the
  yard/truck-only `reserved` lifecycle; a wrong customer site must not reserve;
  a two-asset exchange must not lose either exact slot; reservation evidence
  must remain append-only and visit-attributed; failed receipt/photo evidence,
  stale/collapsed identity, duplicate application, or an out-of-order physical
  stop must not create partial operational effects. These are detected by
  `driver-bin-loaded-exchange.test.js`, the separate adversarial
  `driver-bin-reservation-hardening.test.js`, the established Driver rollback
  case, database uniqueness/state constraints, and the persisted mutation
  runner.
- RED was observed before the production extension. On the inherited source,
  loaded pickup and exchange stopped during assignment because the legacy
  reservation tried to persist the invalid `reserved + customer_site` state.
  The separate wrong-site detector then failed on that same PostgreSQL check
  instead of returning the required `MBT_RESERVATION_SITE_CONFLICT`. Early
  disposable attempts also exposed and corrected a missing-migration run and a
  synthetic UUID cast in test setup before the behavioral RED was frozen; no
  product assertion was weakened.
- GREEN adds one explicit server-derived `exact_hold` mode. Dispatch selects it
  only for a customer-located pickup or a distinct multi-slot exchange. The
  asset service independently revalidates the exact visit slot, asset identity,
  bin type, active state, maintenance state, and customer site. It then appends
  a `reservation_created` movement while preserving the real lifecycle and
  location. The established ordinary one-outgoing delivery path still appends
  its historical `available -> reserved` movement.
- The final fresh combined command over P3.8 feed/assignment/hardening/races,
  all current default-off/schema/ordinary Driver/Dispatch/typed-fleet controls,
  the established asset movement and 50-client reservation suites, and all
  P3-F18–P3-F22 server scenarios completed with exit 0 and exactly **103/103**
  dot-reported tests passing. Within that result, ordinary asset movement and
  reservation behavior is **21/21**, current closed/default controls are
  **40/40**, and Driver BIN execution/rollback/loaded/exchange/site/order
  scenarios are **9/9**. Reversing the three Driver scenario file arguments
  also passed **9/9**.
- Focused c8 over the two changed services ran **60/60** tests and reported
  **98.34% statements, 90.61% branches, 100% functions, and 98.34% lines**.
  `asset-service.js` reported 96.86/91.62/100/96.86 and
  `bin-dispatch-service.js` reported 99.64/90.03/100/99.64 for
  statements/branches/functions/lines. Every new exact-hold, slot, site, mode,
  loop, and same-state movement line executed; disclosed misses are established
  defensive branches outside this packet.
- Persisted manual mutation
  `node test/support/run-p39-reservation-mutations.mjs` used a fresh database
  per mutant and killed **5/5**: legacy-transition substitution, dropped second
  exchange slot, inverted exact asset, skipped customer-site identity, and
  lost reservation-movement visit attribution. The established P3.8 runner
  separately killed **6/6** and restored the same Dispatch source. Final source
  restoration hashes are asset
  `34f5a219293c97e270c2a221aafdd6b9a3b2ef737e5dab5cb1bb58db362a1cd8`
  and Dispatch
  `0bb1448e798928dd585c6af6d9537590eec81532e09711b1fb6894b686f77aa4`.
- Focused TypeScript/JSDoc checking of both changed services and the mutation
  runner exited 0. Focused ESLint exited 0 with zero warnings, and
  `git diff --check` exited 0. The shared whole-P3 typecheck is honestly nonzero
  only for six diagnostics in concurrently developed
  `local-billing-calculator.js`; it reported no diagnostic in this packet.
- Frozen/result hashes are loaded/exchange scenarios
  `4f977dd8f108b537e4d7bd78093c96cff20055f6f26a13661d3172b81c208e07`,
  wrong-site hardening
  `b9a058ed7ff1039d303ae4c801d1ee27ef8942dc1d993cb39b3cbfa05952f37d`,
  synthetic scenario fixture
  `aa9acdea727659cb1ad808cedeaa7f6fb55351c12e3acc3f02f4ba55e1da4ba0`,
  and mutation runner
  `a3275d15fd2e07e796af7d49a5b53ccc735ff6f373195d037843e443533eebf8`.
  All identities and databases were synthetic/disposable. No dependency,
  feature activation, deployment, restart, commit, push, live import, external
  request, NetSuite/Samsara action, or ordinary SCM/Dispatch/Driver/Operator
  workflow mutation occurred. Result: `AUTOMATED COMPLETE / PILOT PENDING`.

## P3.9 Driver PWA BIN client — GREEN packet evidence

This is the Tier 3 client packet for P3-F18–P3-F22. It does not activate the
Driver pilot, deploy the application, or replace the final combined Phase 3
gauntlet.

- The frozen six-assertion client contract first produced **0/6 GREEN** because
  `public/driver-bin-ui.js` and its application-shell entry did not exist. The
  frozen RED hash remains
  `35873d8dfec6c75621524931345c3aa6b7605f1262ec1ade9e70cedf848bbc86`.
  The original two-case Chromium packet likewise failed on the absent BIN UI;
  its RED hash remains
  `371b4dfb49318f56cbf4444b9f85aa73a583091c7b7c755115e4aab2ef94e1b6`.
- GREEN adds a strict `mbt-driver-bin-job-v1` browser boundary, minimum-client
  gate `2026.08.03.1`, requirement-code slots, non-collapsible exact outgoing/
  incoming asset scans, manifest-filtered drafts, required note/photo/signature
  evidence, and frozen dump-site/material/quantity/integer-cent receipt data.
  The BIN module is in the versioned Driver application shell; private API
  responses remain outside Cache Storage.
- Start and completion are local-first even online: the event, original device
  occurrence time, and compressed JPEG Blob evidence enter the existing
  driver/device/manifest/job-partitioned IndexedDB ledger before navigation,
  then use `/api/driver/offline-sync`. They never use the legacy direct BIN
  start/photo-completion path. Full-route plus current-shell readiness protects
  action controls. Offline completion records `not_checked_offline` and makes
  no location request. Reopen/reconnect recovery retains the browser-offline
  marker until a successful current-version response.
- The final pure/static command passed **8/8**: the six frozen work-order/
  completion assertions plus exact pure-JavaScript SHA-256 fallback and a
  Safari decoder-outage case that retains an already compliant JPEG without
  weakening the 2 MiB/2048-pixel bounds. The established Driver offline-client
  and client-version harnesses passed. The PWA i18n harness passed all **746**
  UI keys, including the English/Chinese BIN labels and route/shell warnings.
- The final production-shaped browser packet passed **5**, skipped **1**, and
  failed **0** across desktop Chromium, Pixel-sized Chromium, and iPhone-sized
  WebKit. Desktop and mobile Chromium each completed the full airplane-mode
  scan/note/gallery-Blob/reload/completion/reconnect path and asserted original
  occurrence time, exact evidence descriptors, zero offline GPS, and zero
  legacy endpoints. WebKit passed the online local-ledger start case. Its full
  photo-reload case is explicitly skipped because Linux Playwright WebKit
  itself rejects persisting Blob/File values in IndexedDB with
  `UnknownError: Error preparing Blob/File data to be stored in object store`;
  the WebKit decoder fallback remains unit-covered, while the approved physical
  iPhone Safari pilot remains mandatory before operational acceptance.
- The browser packet was refined after RED only to use a loopback secure-context
  proxy, a reload-persistent synthetic API boundary, and genuine browser/logical
  network loss. The product assertions were not weakened. Current hashes are
  client contract
  `3c627d0ffcc15403382a1e325be33238354a504ca410cf65bf651ecf05250f11`,
  photo hardening
  `37aeacb2a368c13e2409189cdf7096ba1d00769a9148fb95e02c1157cc1c55fa`,
  and browser packet
  `79258393f3ba8ae55953cea112b5679adea252616263ab0d0429bff8958e5efd`.
- Persisted writable-ephemeral client mutation
  `npm run mutate:mbt:p39:client` killed **6/6** plausible defects: schema
  bypass, obsolete-client acceptance, inverted exact-asset identity,
  receipt-balance inversion, unknown draft-field leakage, and suppressed
  immediate ledger sync. It restored targets exactly; runner hash is
  `d394d619bd6f904e797e823982875634f0f5e49b6204ba28f0cfeda763b3d8f6`.
- Full MBT TypeScript/JSDoc checking exited 0. Focused ESLint over all client
  tests/support files exited 0, exact host-file syntax checks exited 0, and
  owned-file `git diff --check` exited 0. The shared whole-P3 lint is honestly
  nonzero only for eight pre-existing curly-rule diagnostics in concurrently
  owned `customer-operations-service.js` and one unused server helper in
  concurrently owned `driver-bin-execution-service.js`; it reports no client
  packet diagnostic.
- All identities and requests were synthetic and disposable. No dependency,
  flag activation, deployment, restart, commit, push, live data, NetSuite/
  Samsara operation, or ordinary SCM/Dispatch/Driver/Operator workflow change
  occurred. Result: `AUTOMATED COMPLETE / PHYSICAL IPHONE PILOT PENDING`.

## P3.9 complete-manifest integrity hardening — GREEN packet evidence

- An independent audit added eight frozen adversarial detectors for boundaries
  not covered by the initial happy-path packet. Before production changes, all
  **8/8 failed behaviorally** with `Missing expected rejection`: locked template
  pre-state, template/derived after-state, exchange customer-site identity,
  earlier-job ordering across visits, completion without a durable start,
  occurrence before manifest generation, occurrence before a newer asset
  movement, and inactive dump-site/material acceptance.
- GREEN processes complete persisted manifests under a manifest/driver advisory
  lock; verifies every earlier route job has a durable completion; requires the
  separate durable start; rejects pre-manifest and stale-asset occurrence times;
  locks and validates every exact asset's template-owned state and exact yard,
  truck, customer, or dump location; and rechecks active dump/material
  acceptance. Exact stored retries return before these checks. The additional
  route/time guards apply only when `manifest.complete === true`, preserving
  partial bootstrap and legacy direct-domain compatibility.
- The focused adversarial suite passes **8/8**. The complete serial P3.9 server
  suite passes **49/49** in 10.1 seconds. Targeted ESLint passes, and the Driver
  packet itself is clean under the full MBT typecheck. Every rejection remains
  inside the existing transaction and leaves no visit, movement, evidence,
  application, reservation, Driver record, or billing effect.
- Files are `src/mbt/driver-bin-execution-service.js`,
  `integration/driver-bin-template-state-guard.test.js`,
  `integration/driver-bin-manifest-integrity.test.js`, and a chronology-only
  fixture adjustment in `integration/driver-bin-offline-queue.test.js`. No
  deployment, restart, live import, feature activation, or external write ran.
  Dedicated persisted mutants for each new decision boundary are required in
  the final P3 extended-mutation stage.

## P3.10 snapshot-only HTTP boundary — RED evidence

- The strengthened HTTP contract is frozen at
  `server/test/mbt/integration/billing-http.test.js`, SHA-256
  `de7fe775bee15dda97998b6515a9d8920c0bd438190a94aabd573f100b6b3abc`.
- Against the pre-change router it produced the required RED result: **4/5
  passed and 1/5 failed**. The failing P3-F26 adapter case returned HTTP 500
  because the router still selected `generateMbbsShadowBilling` while the
  contract exposed only `generateMbbsShadowBillingFromSnapshots` and supplied
  an immutable completed-load snapshot ID. The other authentication, closed-
  gate recovery, request-identity, and no-posting controls remained green.
- The run used a network-disabled disposable container and synthetic actors;
  it performed no deployment, external request, live data operation, or
  feature activation. Assertions were not weakened after RED.

## P3.12 complete migration readiness — RED/GREEN evidence

- The strengthened readiness contract is frozen at
  `server/test/mbt/integration/p3-predeploy-readiness.test.js`, SHA-256
  `a4cc8f29334e27d1c20178484c9738bfd24637723115d910353c5d4218b0a372`.
  It requires every Phase 3 migration from 110 through 121, in addition to
  the Phase 1/2 chain, before deployment readiness can be true.
- RED against the previous readiness tool was exactly **4/5 passed, 1/5
  failed**: the tool stopped at migration 110 and omitted migrations 111–121.
  All closed-environment, exact-flag, authenticated-read, blocked-write, and
  cleanup controls remained green.
- GREEN after extending only `REQUIRED_MBT_P3_MIGRATIONS` was exactly **5/5
  passed**. The test ran in a network-disabled disposable container and made
  no deployment, database mutation, external request, feature activation, or
  live-data access.

## P3.10 P3-F23/P3-F24/P3-F28 pilot reconciliation — GREEN packet evidence

This is Tier 3 evidence for retained, immutable pilot comparison and variance
resolution. It does not enable billing, activate a pilot, or replace the final
combined Phase 3 gauntlet.

- The frozen four-scenario contract first produced the required **0/4 RED**
  because `createPilotReconciliationBatch` did not exist. The separate
  adversarial packet then exposed three intended faults: a non-billing actor
  could create evidence, a hostile numeric-string distance was accepted, and
  an empty correction reference was accepted. A later eight-row randomized
  comparison exposed nondeterministic same-kind ordering. No frozen assertion
  was weakened; one proposed adversarial assertion was withdrawn because it
  contradicted the frozen requirement to retain and compare a manual receipt
  total independently from the application total.
- GREEN persists application and manual snapshots separately with hashes,
  exact typed comparison for movement, receipt, distance, billing-line, and
  cross-charge-allocation evidence, deterministic ordering, strict billing
  authority, atomic batch creation, bounded cursor pagination, and idempotent
  command audit. Distance notes are required only when the delta is strictly
  greater than `max(2 km, 5%)`. Resolution is append-only under row locking;
  concurrent decisions produce exactly one winner, retain the original
  evidence, and require a typed UUID correction reference where applicable.
- Migration 119 retains signed estimated-tax support while constraining
  nonnegative subtotal/total money and permits null approval fields only for
  draft billing versions. Immutable snapshot/resolution behavior and duplicate
  identities are enforced directly by the fresh PostgreSQL integration suite.
- On a database recreated from empty and migrated through migrations 001–121,
  the frozen and hardening packets completed with exit 0 and exactly **15/15**
  tests passing: four frozen scenarios and eleven independent adversarial,
  atomicity, authorization, ordering, retry, pagination, malformed-input, and
  15-client concurrency scenarios.
- Focused c8 for `pilot-reconciliation-service.js` reported **99.19%
  statements, 94.90% branches, 100% functions, and 99.19% lines**. Disclosed
  misses are defensive branches at lines 197–198, 301–302, and 715–716.
- Persisted writable-ephemeral mutation
  `node test/support/run-p310-reconciliation-mutations.mjs` killed **6/6**
  service defects: authority bypass, numeric-string coercion, inclusive
  distance threshold, inverted exact comparison, empty correction reference,
  and randomized UUID ordering. Sources were restored exactly. Migration
  immutability is covered directly in PostgreSQL because the mutation image
  intentionally mounts migrations read-only.
- Focused ESLint exited 0 with zero warnings; all three owned JavaScript files
  passed `node --check`; owned-file `git diff --check` exited 0; and the parent
  whole-P3 lint independently exited 0. The shared full typecheck is honestly
  nonzero only for two diagnostics in the separately owned
  `run-p310-adversarial-mutations.mjs`; it reports no diagnostic in this
  reconciliation packet.
- Final SHA-256 hashes are migration 119
  `9fc4dd97c44c5aaf0672eb04ae4e995f465e139b5578d8f83a8327e8dcd1d087`,
  service
  `fd4cc94f3470e96c46464dfb9d2b3f6d6470dd1829f8ce975103278fae0bb558`,
  adversarial packet
  `43a78357cbc9a66a5f7d9156198ac02a630f14dd5d46be3c861f0dd31b4c27ff`,
  and mutation runner
  `124c5b530cbc5f188458a24ffcd29d3054ca708e5e87d0c157fa897f5f4a91c8`.
  All identities and databases were synthetic/disposable. No feature flag,
  dependency, deployment, restart, commit, push, live import, external request,
  NetSuite/Samsara write, or ordinary SCM/Dispatch/Driver/Operator workflow
  mutation occurred. Result: `AUTOMATED COMPLETE / PILOT PENDING`.

## P3.10 local shadow-billing adversarial packet — GREEN evidence

- The first isolated adversarial run completed **12** checks with **10 passing
  and 2 failing**. The two RED boundaries were exact: PostgreSQL `42P01` for
  the absent durable completed-load snapshot relation, and a rate-component
  constraint that could not represent `discount`. The same packet also guarded
  billing authority, exact visit ownership, component currency, audited
  distance overrides, dump-photo identity/hash, selected rate band, immutable
  dedupe evidence, unknown quantities, approval line identity/count, and signed
  discount tax.
- Migration 121 now provides immutable server-owned completed-load snapshots
  and discount rate-component support. A separately frozen boundary check
  proves the public generator rejects a present caller-authored `loads` field
  with `MBT_CROSS_CHARGE_SNAPSHOT_INPUT_INVALID`, writes no case on rejection,
  then generates from a retained snapshot ID and freezes that ID and its values
  in cross-charge source evidence.
- On a PostgreSQL database recreated from empty and migrated through **001–121**,
  the final frozen packet passed exactly **14/14**, failed **0**, skipped **0**,
  in **21284.130194 ms**. The packet also proves a visit cannot be billed
  before durable completion. Main detector SHA-256 is
  `f14e482827fb9a341344717031fafda7066b4d0fb12f3aa232f6ebcf8fe2b797`;
  snapshot-boundary detector SHA-256 is
  `3e11d785d865fc9f4d5652708e3f404cabb902855cafbb398ff318eb9f067400`.
- The final writable-ephemeral mutation run killed **15/15** independent
  defects: authorization bypass, cross-visit evidence use, pre-completion
  billing, component-currency drift, ignored distance override, lost
  receipt-photo hash, absent rate band, dedupe evidence drift, ignored unknown
  quantity, approval count bypass, invalid signed-tax constraint, mutable
  completed-load snapshot, rejected discount schema, accepted caller load, and
  lost snapshot identity. Every mutant used its own database and all writable
  source targets were restored exactly. Mutation-runner SHA-256 is
  `84bf6648986a3c8dd76bd0c0ebfb7abcafeca31f772e1bdcc251324e2ac548fe`.
- Focused ESLint completed with zero warnings, and the full MBT TypeScript/JSDoc
  check exited 0 after a type-only runner narrowing. All fixtures, databases,
  and actors were synthetic/disposable. This review performed no deployment,
  feature activation, external request, live-data operation, NetSuite/Samsara
  write, or ordinary SCM/Dispatch/Driver/Operator workflow mutation. Result:
  `AUTOMATED COMPLETE / PILOT PENDING`.

## P3 Dispatch BIN capability discovery — independent GREEN review

This bounded Tier 3 review covers the authenticated public status boundary. The
approved failure model was environment-gate bypass, database-only activation,
pilot-role leakage, a false operational indicator, or loss of the legacy
`mbt-v1` response. It made no production edit; its only executable addition is
the independent HTTP integration detector.

- The frozen HTTP detector is
  `test/mbt/integration/dispatch-bin-status-capability.test.js`, SHA-256
  `82965c4bb3324e2918cf44c5e35e83842b4be3cf28794d8ae6a1af8ef412bc2a`.
  The pre-existing static detector remained unchanged at SHA-256
  `33a9f57464a074e25732a5e6084e66c0ed416ee841b1fa096929f82007809988`.
  Final reviewed router SHA-256 was
  `456848942d08cd4f5f1cc92e3419ccf66df69adef1fc1fc24f92a1d7d89085eb`.
- A PostgreSQL database isolated with `--network none` was created from empty
  and migrated successfully through migrations **001–121**. Against the final
  image, the unchanged static discovery detector passed **1/1**, and the real
  authenticated HTTP matrix passed **5/5**, failed **0**, skipped **0**. It
  proved: closed legacy defaults retain the `mbt-v1` fields and capability
  envelope; database root plus BIN true cannot bypass a false
  `MBT_BIN_DISPATCH_ENABLED`; environment plus database gates enable both
  Dispatcher and Admin; an ordinary Operator receives `pilot_scope_denied`;
  and disabled/pilot-denied responses remain `operational: false`.
- Final writable-ephemeral manual mutation killed **3/3** plausible public-API
  defects: skip the specific Phase 3 authorizer branch, authorize every caller
  role, and force `operational: true`. The expected assertion failures were
  observed against separate disposable image copies; no repository source was
  mutated. A persistent mutation runner was not added because this review was
  explicitly limited to a new detector file plus evidence, so reproduction of
  these three manual substitutions is correspondingly command-level rather
  than a repository entry point.
- Full `npm run typecheck:mbt` exited 0. Focused ESLint over the router and both
  detectors exited 0 with zero warnings. Full `npm run secrets:mbt` reported
  **41 new paths checked and no high-confidence findings**. The credential in
  the test is synthetic and carries the scanner's narrow same-line isolated-
  fixture marker. No dependencies changed, so supply-chain/license review was
  not applicable; property/fuzz testing was not added because this boundary is
  the finite boolean/role matrix asserted directly. Standalone changed-line
  coverage was not claimed because the reviewer changed no production line;
  actual HTTP execution and mutation sensitivity cover the reviewed decisions.
- During the final freeze, mutation preparation detected a concurrent helper
  extraction that moved the authorizer outside the already-frozen static route
  contract. The route owner restored the exact inline boundary before the
  authoritative runs; neither detector was weakened. All identities and data
  were synthetic/disposable. No deployment, restart, commit, push, live import,
  external request, feature activation, NetSuite/Samsara action, or ordinary
  SCM/Dispatch/Driver/Operator workflow mutation occurred. Result:
  `INDEPENDENT AUTOMATED REVIEW PASS / PILOT STILL PENDING`.
