# Evidence Report — MBT Bin Operations Phase 1 Foundation (Tier 3)

## Acceptance record

- Spec approval: obtained from the user through the explicit request to
  implement Phase 1 with `$old-coder`.
- Approved specification: `docs/mbt/01-executable-spec.md`, including the
  append-only P1-R1 and P1-R2 non-regression clarifications.
- Baseline commit: `37dfafbf534268aa84832c0f7a677f5f6a656bf6`.
- Implementation source state:
  `sha256:15bbf5fe6d27e511996fb9848b3060998617393cdde05c0ef8a27e9c99a43898`.
  This deterministic
  tree hash covers every tracked and non-ignored untracked source file except
  this self-referential evidence report and generated `server/test-artifacts`.
- Commit status: intentionally uncommitted; the user did not request a commit.
  The baseline commit plus the source-state hash identifies the tested tree.
- Toolchain: Node 20 pinned by image digest, PostgreSQL 18 pinned by image
  digest, and exact development versions in `server/package-lock.json`.
- Source-state recipe (run from the repository root; the evidence report is
  excluded to avoid a self-referential hash):

  ```bash
  git ls-files --cached --others --exclude-standard -z | while IFS= read -r -d '' mbt_path; do case "$mbt_path" in server/test-artifacts/*|docs/mbt/evidence/P1.md) continue ;; esac; if [[ -L "$mbt_path" ]]; then printf '%s\0symlink\0%s\0' "$mbt_path" "$(readlink -- "$mbt_path")"; elif [[ -f "$mbt_path" ]]; then printf '%s\0%s\0%s\0' "$mbt_path" "$(stat -c '%a' -- "$mbt_path")" "$(sha256sum -- "$mbt_path" | cut -d ' ' -f 1)"; fi; done | sha256sum | cut -d ' ' -f 1
  ```

- Reproduction entry point:

  ```bash
  MBT_GAUNTLET_BASE_SHA=37dfafbf534268aa84832c0f7a677f5f6a656bf6 bash server/tools/mbt-gauntlet.sh P1
  ```

- Isolation: the entry point uses only `docker-compose.mbt-test.yml`, an
  internal Docker network, a tmpfs PostgreSQL database, dummy credentials,
  read-only application filesystems, and disposable containers. It refuses
  the production Compose project and does not mount production environment or
  persistent data.

## Result and safety boundary

Phase 1 is implemented and the final fresh gauntlet passed. It adds foundation
schema, controlled read surfaces, audit/idempotency/concurrency primitives,
and deployment readiness checks. It does not enable an MBT operation.

- `MBT_ENABLED` and `MBT_NETSUITE_WRITES_ENABLED` default false.
- Every operational `mbt_feature_flags` seed is false.
- Front Desk commands, billing operations, BIN Dispatch, Driver BIN execution,
  customer synchronization, external scheduling, and production NetSuite
  writes remain unavailable.
- The Phase 1 NetSuite boundary is injected and read-only.
- Disabled BIN rejection occurs before Dispatch, Driver, domain, audit,
  receipt, reservation, or outbox state changes.
- Existing Operator, Dispatcher, Admin, SCM, Yard Manager, Sales, Driver/PWA,
  Dispatch, Return, and Smart SCM behavior remains in the legacy baseline.
  The unrelated Smart SCM shipping method named `MBT` is unchanged.
- No production Compose file, production environment, live database, external
  account, or production data was changed during implementation or testing.

## Specification to test mapping

| Scenario | Executable verification | Status |
|---|---|---|
| F01 — capabilities fail closed | `unit/pure-contracts`, `unit/config-legacy-seams`, `integration/http-capabilities`, `integration/dispatch-bin-disabled` | pass |
| F02 — live multi-role authorization | `integration/authority-roles`, `integration/http-role-matrix`, `unit/ui-navigation-contract`, `e2e/controlled-shell` | pass |
| F03 — optimistic concurrency | `unit/pure-contracts`, `integration/command-audit-repositories`, `integration/http-capabilities` | pass |
| F04 — command idempotency | `unit/pure-contracts`, `property/pure-contracts.property`, `integration/command-audit-repositories`, `concurrency/command-idempotency` | pass |
| F05 — immutable, redacted audit | `unit/foundation-services`, `integration/foundation-schema`, `integration/command-audit-repositories` | pass |
| F06 — seeds and legacy truck safety | `integration/foundation-schema`, `integration/migration-upgrade` | pass |
| F07 — exact asset state and reservation | `integration/asset-movements`, `concurrency/asset-reservations` | pass |
| F08 — append-only movements | `integration/asset-movements` | pass |
| F09 — rate bands and historical pricing | `unit/pure-contracts`, `property/pure-contracts.property`, `integration/rate-lifecycle`, `integration/immutable-financial-evidence` | pass |
| F10 — contracts, visits, and templates | `integration/visit-foundation`, `integration/rate-lifecycle`, `concurrency/asset-reservations` | pass |
| F11 — billing and cross-charge safety | `integration/deposit-foundation`, `integration/immutable-financial-evidence`, `integration/outbox-preflight` | pass |
| F12 — durable outbox foundation | `unit/foundation-services`, `integration/outbox-preflight`, `concurrency/outbox-claims`, `integration/immutable-financial-evidence` | pass |
| F13 — NetSuite readiness foundation | `unit/foundation-services`, `integration/outbox-preflight`, `integration/predeploy-readiness` | pass |
| F14 — Dispatch BIN safety seam | `contracts/mbt-v1.schema`, `unit/dispatch-bin-safety`, `integration/dispatch-bin-disabled`, `integration/driver-bin-disabled`, `integration/dispatch-assignment-bin-scan`, `integration/predeploy-readiness` | pass |
| F15 — controlled UI and negative scope | `integration/http-shell`, `integration/http-role-matrix`, `unit/ui-navigation-contract`, `e2e/controlled-shell` | pass |
| F16 — migration and real execution | `integration/foundation-schema`, `integration/migration-upgrade`, `integration/http-capabilities`, production-image health and predeploy stages | pass |
| Must not change established application behavior | full 106-harness legacy baseline, 39 browser cases, route/token contracts, legacy public-script syntax | pass |
| Must not alter Smart SCM `method = 'MBT'` | fresh-schema and schema-101 upgrade assertions plus Smart SCM legacy harnesses | pass |
| Must not touch production services or data | isolated Compose verifier, CI workflow contract, disposable stack, production-root freeze assertions | pass |
| Must not introduce a production write capability | read-only adapter contract, capability tests, disabled flags, production predeploy | pass |

Paths in this table are relative to `server/test/mbt/` unless stated
otherwise. The detailed historical RED to GREEN journal is retained in
`docs/mbt/00-implementation-index.md`.

## Final fresh gauntlet — 2026-08-03

All results below came from the single final entry-point run after the last
implementation and test edit.

| Layer | Persisted command/stage | Exact result |
|---|---|---|
| Isolation contract | gauntlet `verify-isolated-compose.mjs` stage | passed; dedicated project, internal network, tmpfs DB, no production mounts/data |
| CI least privilege | gauntlet workflow verifier and collector contract | static contract 3/3; actual workflow passed; base/dirty/untracked/exclusion/fail-closed/secret fixture passed |
| Full-diff secret scan | gauntlet collector plus `scan-diff-secrets.mjs` | 95 new paths and changed-line diff scanned; 0 high-confidence findings |
| Production dependency tree | gauntlet production-image `npm ls --omit=dev --all` stage | exit 0; omit-dev runtime tree resolved |
| Fresh migrations | gauntlet migration stage | migrations 001–107 applied in order to fresh PostgreSQL 18 |
| Representative upgrade | `test:mbt` / `integration/migration-upgrade` | schema-101 state preserved; official runner idempotent; pass |
| Main test suite | `npm run test:mbt` | 187 passed, 0 failed, 0 skipped |
| Suite order health | `npm run test:mbt:shuffled` | 3 seeds (`2026080301`, `2026080337`, `2026080399`), 33 files and 187/187 tests per seed |
| Static types | `npm run typecheck:mbt` | 0 errors |
| Lint/complexity | `npm run lint:mbt` | 0 warnings, 0 errors |
| Legacy browser syntax | `npm run syntax:legacy` | `login.js`, `control.js`, and `app-sidebar.js` parsed successfully |
| Coverage | `npm run test:mbt:coverage` | 4,462/4,535 statements and lines (98.39%); 181/182 functions (99.45%); 804/864 branches (93.05%) |
| Property testing | property group inside `npm run test:mbt` | 5 properties × 1,000 generated cases; all passed with fixed recorded seeds |
| Mutation | `npm run mutate:mbt` in the ephemeral writable mutation image | 14/14 plausible critical mutants killed (100%); every source restoration hash matched |
| Dependency licenses | `npm run licenses:mbt` | 387 packages checked; allowlist passed; one documented pre-existing metadata exception |
| Existing application regression | `npm run test:baseline:mbt:full` | 106/106 legacy harnesses passed |
| Real production-image execution | gauntlet runtime startup and health stage | `{"ok":true,"app":"MBBS Yard Server"}` under `NODE_ENV=production` with MBT gates closed |
| Deployment preflight | `npm run preflight:mbt-p1-deploy` in production image | `ready: true`; no missing migrations, enabled flags, or Dispatch collisions |
| Browser and accessibility | `npm run test:mbt:e2e` | 39/39 passed: desktop Chromium, mobile Chromium, mobile WebKit; serious/critical axe violations: 0 |
| Source integrity | gauntlet `git diff --check` stage | passed |
| Vulnerability audit | gauntlet `npm audit --audit-level=high` stage | 0 vulnerabilities |

Generated final-run artifacts are intentionally ignored by Git and available
locally at `server/test-artifacts/coverage/coverage-summary.json` and
`server/test-artifacts/playwright/report.json` until the next fresh gauntlet,
which deletes stale artifacts before it starts.

After this report and the completion index were written, a supplemental
read-only integrity pass reproduced the recorded source-state hash, passed
`git diff --check` and Bash syntax, confirmed production Compose/environment/
data roots remained clean, and scanned 96 new paths plus changed lines with
zero high-confidence secret findings. Documentation-only edits occurred after
the final all-stage run; no implementation or test source changed.

## Dependencies and capability review

The exact-pinned development-only additions are `c8`, `fast-check`, `eslint`,
`typescript`, `@types/node`, `@types/express`, `@types/pg`, `ajv`,
`@playwright/test`, and `@axe-core/playwright`. Their justifications were
approved in the executable specification. Production installation continues
to use `npm ci --omit=dev`.

Two production transitive overrides are explicit security-only repairs:
`brace-expansion` 1.1.17 under `minimatch` 3.1.5 and 2.1.3 under `minimatch`
5.1.9. The production tree and affected spreadsheet/archive legacy harnesses
passed. The license checker retains one pre-existing exception for
`buffers@0.1.1`, whose package and lock metadata do not declare a license;
manual legal review remains required.

The capability diff adds only the approved database foundation, read-only MBT
status/configuration surfaces, isolated test/CI subprocesses, and the read-only
deployment preflight. It adds no production scheduler, external write adapter,
live NetSuite/Samsara access, or operational BIN route.

## Honest notes and known limits

- Earlier RED runs and their repairs are recorded append-only in
  `docs/mbt/00-implementation-index.md`; assertions were not weakened to reach
  GREEN.
- The first post-CI final gauntlet stopped before image build because the real
  repository base SHA leaked into the collector harness's unrelated temporary
  Git fixture. An additive regression test failed 2/3, the harness cleared the
  inherited fixture value, and its focused suite passed 3/3. The entire final
  gauntlet was then restarted from fresh artifacts and passed.
- One ad hoc focused harness command used relative Docker mount paths and
  failed its scanner-output assertion. The persisted canonical command with
  absolute defaults passed without changing source or assertions.
- A preliminary browser run exposed a test-only intercepted-login navigation
  race. The final helper passively observes the real response and committed
  browser storage; it passed 30/30 focused repetitions before the final 39/39
  browser run.
- PostgreSQL reports pre-existing `pg` deprecation warnings in several legacy
  harnesses. They did not fail the baseline and are not caused by Phase 1.
- Zero-downtime DDL under concurrent production traffic is not claimed.
  Migrations 102 and 104 need a maintenance window or bounded lock monitoring
  with abort-and-retry before starting the new image.
- Live production migration, external NetSuite writes, customer sync, billing,
  BIN Dispatch, and Driver/PWA BIN execution were deliberately not run: they
  are outside or prohibited by Phase 1. Their status is not operationally
  verified, and every corresponding gate remains closed.
- Browser functional/accessibility evidence covers the pinned Chromium and
  WebKit targets. It is not a screenshot-perfect visual-regression claim and
  does not replace later real-device acceptance for operational phases.

Within the approved Phase 1 specification, every required gauntlet layer is
green. This is evidence of the expressed constraints, not a claim that the
specification contains every possible future requirement.

## Superseding deployment-repair gauntlet — 2026-08-03

The first production read-only preflight was intentionally run before any
migration or application replacement. It exposed an unbounded-memory defect:
the scanner attempted to materialize all current and historical Dispatch JSON
at once and was killed at the Node.js heap limit. Production remained healthy
at schema 101, with no Phase 1 database write.

P1-R3 now scans the two snapshot tables sequentially by their qualified bigint
primary keys, with a hard page size of one. Each row is inspected immediately
with the existing reserved-BIN semantic oracle; only compact collision
summaries persist. Numeric cursors must advance or the gate fails closed.
Production sizing measured 42 current rows plus 1,454 history rows,
8,347,259,171 logical JSON bytes in total, and an 11,090,706-byte largest
logical row. PostgreSQL `EXPLAIN` confirmed primary-key index scans after the
qualified-order repair.

After the last source/test edit, the complete gauntlet was restarted from the
beginning and passed:

| Layer | Exact result |
|---|---|
| Source identity | `15bbf5fe6d27e511996fb9848b3060998617393cdde05c0ef8a27e9c99a43898` on `codex/dockerVer` at baseline commit `37dfafbf534268aa84832c0f7a677f5f6a656bf6` |
| Main and order health | 190/190; three fixed shuffled seeds each 190/190 |
| Focused P1-R3 | bounded late-page collision, sparse `1 → 2 → 10` bigint keyset, and non-advancing fail-closed cases passed |
| Static checks | typecheck 0 errors; lint 0 warnings/errors; legacy public syntax passed; `git diff --check` passed |
| Coverage | 98.39% statements/lines; 99.45% functions; 93.05% branches |
| Mutation | 17/17 killed (100%), including page inflation, text-alias ordering, and keyset-predicate removal; every restoration hash matched |
| Legacy application | 106/106 harnesses passed |
| Production-equivalent runtime | image `sha256:888fd4225ea3328e0b103f879fb78cb976dfa70492b301d697347bdb39898895` healthy; isolated preflight `ready: true` with empty missing/flag/collision lists |
| Browser/accessibility | 39/39 desktop Chromium, mobile Chromium, and mobile WebKit; no serious/critical axe violations |
| Supply chain | 387-package license gate passed with the documented pre-existing exception; 0 high-confidence secret findings; 0 vulnerabilities |

This supersedes the earlier 187-test/14-mutant final-run counts. It does not
enable an MBT capability or authorize a Phase 2 operation.

## Production deployment record — 2026-08-03

The user explicitly authorized deployment on `codex/dockerVer`. The generic
VM updater was not used because it would reject the intentionally uncommitted
tree, pull the pre-Phase-1 remote baseline, and execute unrelated Dispatch V2
data-changing steps.

- Baseline commit: `37dfafbf534268aa84832c0f7a677f5f6a656bf6`.
- Deployed source state:
  `15bbf5fe6d27e511996fb9848b3060998617393cdde05c0ef8a27e9c99a43898`.
- Deployed production image:
  `sha256:888fd4225ea3328e0b103f879fb78cb976dfa70492b301d697347bdb39898895`.
  The exact gauntlet runtime image was tagged directly for both Compose `app`
  and `migrate`; it was not rebuilt after the final gauntlet.
- Rollback application image:
  `sha256:77ef0c6d365da490cb9703ee9fe15421d5002791878ef866bc53d9521d3b6731`
  (`mbbs-operator-app-app:pre-mbt-p1-live-20260803`). Docker had discarded
  the running container's old image metadata, so its filesystem was exported,
  stripped of mounted `.env`, `.env.old`, and application-data content, and
  re-imported with the original user/workdir/entrypoint/command metadata.
  The retained sanitized rootfs archive SHA-256 is
  `5413a999cf0359f717e3bb5fdeb4fd521d09eed321c1a782024a1c9f71819245`;
  a value-aware scan found zero mounted sensitive values.
- Fresh database backup:
  `docker/backups/mbbs-before-mbt-p1-20260803T122439Z.dump`, 534,827,704
  bytes, SHA-256
  `83de08245b2e4f2e417e602e41278d358f1cba6f2632384bfc52b946ca0bfc46`.
  `pg_restore --list` passed and container/host hashes matched.
- The backup was fully restored with `--exit-on-error` into a network-isolated
  disposable PostgreSQL 18 volume. Aggregate validation recovered 102 schema
  migration rows through migration 101, 42 current snapshots, 1,454 history
  snapshots, 10 operators, and 8 Dispatch trucks. The disposable container
  and volume were then removed; the host backup remains.
- Only the application was stopped. PostgreSQL and Ollama remained healthy.
  Immediately before migration there were zero active other sessions, zero
  idle-in-transaction sessions, and no competing migration runner.
- Migrations 102–107 applied once with `lock_timeout=5s`,
  `statement_timeout=15min`, and a unique migration application name. All six
  filenames are recorded; the operator/truck row counts remained 10/8.
- The repaired production preflight ran with a 30-second per-statement limit
  and 15-minute process deadline and returned exactly `ready: true`,
  `missingMigrations: []`, `enabledFlags: []`, and
  `dispatchCollisions: []`.
- Both environment gates resolved false before cutover. After startup, zero
  database feature flags were enabled. Front Desk and Billing retained their
  disabled Phase 1 copy.
- The upper-bound observed app maintenance interval was 12:37:22Z through the
  first successful retained health check at 12:41:57Z (4 minutes 36 seconds).
  The replacement container has restart count zero and is healthy.
- `/`, `/control`, `/operator`, `/dispatch`, `/scm`, `/sales`, `/driver`, all
  three MBT shells, and both MBT assets returned 200. `/api/mbt/status` and
  `/api/operators` returned 401. The Driver endpoint correctly returned 426
  without the required current PWA version and 401 after the current version
  header was supplied; `/api/driver/client-version` returned 200.
- Recent production logs contained no fatal, unhandled, uncaught, out-of-memory,
  connection-refused, or migration-failure signal.

No MBT operational flag, NetSuite write, customer synchronization, billing
operation, BIN Dispatch, or Driver BIN workflow was enabled or exercised.
Database rollback remains restore-based because Phase 1 has no down migrations;
an application-image rollback alone intentionally leaves the additive,
fail-closed schema in place.
